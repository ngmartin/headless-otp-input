import { test, describe } from 'vitest'

describe('Props Testing', () => {
  test('should call onCompleted when completed', () => {})

  test('should allow preventDefault on onCompleted', () => {})
})
